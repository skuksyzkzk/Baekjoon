#include <iostream>

using namespace std;

main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long num1, num2, num3;

    cin >> num1 >> num2 >> num3;
    cout << num1 + num2 + num3;
}